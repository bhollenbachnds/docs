---
- title: NDS.Services.Globalization
---

## Aperçu

Fournit un support de localisation pour PADS4 : fuseaux horaires, cultures, formats de date/heure et gestion des ressources multilingues. Utilisé par d'autres services et l'interface utilisateur pour gérer le formatage régional.

| Champ                                 | Valeur                                                                         |
| ------------------------------------- | ------------------------------------------------------------------------------ |
| Swagger UI                            | `https://<your-server>/rdx/NDS.Services.Globalization/swagger/index.html`      |
| Spécification OpenAPI                 | `https://<your-server>/rdx/NDS.Services.Globalization/swagger/v1/swagger.json` |
| URL de base                           | `https://<your-server>/rdx/NDS.Services.Globalization`                         |
| Nombre total de points de terminaison | 19                                                                             |

***

## Authentification

Tous les points de terminaison utilisent le cookie de session PADS4 standard. Obtenez-le une fois et réutilisez-le :

```bash
# Log in and save the session cookie
curl -c cookies.txt -X POST 'https://<your-server>/api/v1/Account/Logon' \
  -H 'Content-Type: application/json' \
  -d '{"UserName": "admin", "Password": "yourpassword", "Domain": "yourdomain"}'

# Use the cookie on all subsequent requests
curl -b cookies.txt -X GET 'https://<your-server>/rdx/<ServiceName>/...'
```

> Remplacez `https://<your-server>` par l'adresse de votre serveur PADS4 et `<ServiceName>` par le chemin du service indiqué ci-dessus.

***

## Référence des points de terminaison

| Méthode | Chemin                                        | Description                                      |
| ------- | --------------------------------------------- | ------------------------------------------------ |
| `GET`   | `/api/v1/Globalization/SupportedLanguages`    | Obtenir les langues prises en charge             |
| `GET`   | `/api/v1/Globalization/Cultures`              | Obtenir toutes les cultures                      |
| `GET`   | `/api/v1/Globalization/TimeZones`             | Obtenir les fuseaux horaires                     |
| `GET`   | `/api/v1/Globalization/ServerCulture`         | Obtenir la culture du serveur                    |
| `GET`   | `/api/v1/Globalization/ServerTimeZone`        | Obtenir le fuseau horaire du serveur             |
| `GET`   | `/api/v1/diagnostics/toggles`                 | Obtenir les bascules                             |
| `GET`   | `/api/v1/diagnostics/configuration`           | Obtenir la section de configuration              |
| `GET`   | `/api/v1/diagnostics/configurationex`         | Obtenir la configuration                         |
| `GET`   | `/api/v1/diagnostics/circuitbreakers`         | Obtenir les disjoncteurs                         |
| `GET`   | `/api/v1/diagnostics/circuitbreakers/reset`   | Réinitialiser le disjoncteur                     |
| `GET`   | `/api/v1/diagnostics/circuitbreakers/isolate` | Isoler le disjoncteur                            |
| `POST`  | `/api/v1/diagnostics/events`                  | Obtenir les événements                           |
| `GET`   | `/api/v1/health`                              | Obtenir                                          |
| `GET`   | `/api/v1/health/clean`                        | Obtenir propre                                   |
| `POST`  | `/api/v1/Management/RequeueErrorMessages`     | Remettre en file d'attente les messages d'erreur |
| `POST`  | `/api/v1/Management/PurgeErrorMessages`       | Purger les messages d'erreur                     |
| `GET`   | `/migration`                                  | Obtenir les migrations                           |
| `POST`  | `/migration`                                  | Exécuter la migration                            |
| `GET`   | `/version`                                    | Obtenir                                          |

***

## Exemples de requêtes & réponses

### GET `/api/v1/Globalization/SupportedLanguages`

**Request:**

```bash
curl -b cookies.txt -X GET 'https://<your-server>/rdx/NDS.Services.Globalization/api/v1/Globalization/SupportedLanguages'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true}
```

***

### GET `/api/v1/Globalization/Cultures`

**Request:**

```bash
curl -b cookies.txt -X GET 'https://<your-server>/rdx/NDS.Services.Globalization/api/v1/Globalization/Cultures'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true}
```

***

### GET `/api/v1/Globalization/TimeZones`

**Request:**

```bash
curl -b cookies.txt -X GET 'https://<your-server>/rdx/NDS.Services.Globalization/api/v1/Globalization/TimeZones'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true}
```

***

### GET `/api/v1/Globalization/ServerCulture`

**Request:**

```bash
curl -b cookies.txt -X GET 'https://<your-server>/rdx/NDS.Services.Globalization/api/v1/Globalization/ServerCulture'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true}
```

***

### GET `/api/v1/Globalization/ServerTimeZone`

**Request:**

```bash
curl -b cookies.txt -X GET 'https://<your-server>/rdx/NDS.Services.Globalization/api/v1/Globalization/ServerTimeZone'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true}
```

***
