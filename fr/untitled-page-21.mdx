---
- title: NDS.Services.Device
---

## Aperçu

Gère les appareils de visualisation PADS4 : enregistrement, regroupement, attribution de propriétés et licences. Registre central pour tout le matériel d'affichage connecté à un déploiement PADS4.

| Champ                                 | Valeur                                                                  |
| ------------------------------------- | ----------------------------------------------------------------------- |
| Swagger UI                            | `https://<your-server>/rdx/NDS.Services.Device/swagger/index.html`      |
| OpenAPI spec                          | `https://<your-server>/rdx/NDS.Services.Device/swagger/v1/swagger.json` |
| URL de base                           | `https://<your-server>/rdx/NDS.Services.Device`                         |
| Nombre total de points de terminaison | 29                                                                      |

***

## Authentification

Tous les points de terminaison utilisent le cookie de session PADS4 standard. Obtenez-le une fois et réutilisez-le :

```bash
# Log in and save the session cookie
curl -c cookies.txt -X POST 'https://<your-server>/api/v1/Account/Logon' \
  -H 'Content-Type: application/json' \
  -d '{"UserName": "admin", "Password": "yourpassword", "Domain": "yourdomain"}'

# Use the cookie on all subsequent requests
curl -b cookies.txt -X GET 'https://<your-server>/rdx/<ServiceName>/...'
```

> Remplacez `https://<your-server>` par l'adresse de votre serveur PADS4 et `<ServiceName>` par le chemin de service indiqué ci-dessus.

***

## Référence des points de terminaison

| Méthode  | Chemin                                        | Description                                      |
| -------- | --------------------------------------------- | ------------------------------------------------ |
| `POST`   | `/api/v1/device`                              | Obtenir les appareils                            |
| `POST`   | `/api/v1/device/sendCommand`                  | Envoyer une commande                             |
| `DELETE` | `/api/v1/device/removeDevices`                | Supprimer un appareil                            |
| `POST`   | `/api/v1/device/updateData`                   | Mettre à jour les données                        |
| `POST`   | `/api/v1/device/update`                       | Mettre à jour l'appareil                         |
| `POST`   | `/api/v1/device/search`                       | Rechercher des appareils                         |
| `POST`   | `/api/v1/device/getState`                     | Obtenir l'état                                   |
| `GET`    | `/api/v1/device/stats`                        | Obtenir les statistiques de l'appareil           |
| `POST`   | `/api/v1/gateway/register`                    | Enregistrer la passerelle                        |
| `POST`   | `/api/v1/gateway/updateConfig`                | Mettre à jour la configuration                   |
| `POST`   | `/api/v1/gateway`                             | Obtenir les passerelles                          |
| `POST`   | `/api/v1/gateway/update`                      | Mettre à jour                                    |
| `POST`   | `/api/v1/gateway/logs`                        | Obtenir les journaux                             |
| `POST`   | `/api/v1/gateway/delete`                      | Supprimer                                        |
| `POST`   | `/api/v1/webhook/smartthings/{domain}`        | Smart Things Webhook                             |
| `GET`    | `/api/v1/diagnostics/toggles`                 | Obtenir les bascules                             |
| `GET`    | `/api/v1/diagnostics/configuration`           | Obtenir la section de configuration              |
| `GET`    | `/api/v1/diagnostics/configurationex`         | Obtenir la configuration                         |
| `GET`    | `/api/v1/diagnostics/circuitbreakers`         | Obtenir les disjoncteurs                         |
| `GET`    | `/api/v1/diagnostics/circuitbreakers/reset`   | Réinitialiser le disjoncteur                     |
| `GET`    | `/api/v1/diagnostics/circuitbreakers/isolate` | Isoler le disjoncteur                            |
| `POST`   | `/api/v1/diagnostics/events`                  | Obtenir les événements                           |
| `GET`    | `/api/v1/health`                              | Obtenir                                          |
| `GET`    | `/api/v1/health/clean`                        | Obtenir (nettoyé)                                |
| `POST`   | `/api/v1/Management/RequeueErrorMessages`     | Remettre en file d'attente les messages d'erreur |
| `POST`   | `/api/v1/Management/PurgeErrorMessages`       | Purger les messages d'erreur                     |
| `GET`    | `/migration`                                  | Obtenir les migrations                           |
| `POST`   | `/migration`                                  | Exécuter la migration                            |
| `GET`    | `/version`                                    | Obtenir                                          |

***

## Exemples de requêtes et de réponses

### POST `/api/v1/device`

**Request:**

```bash
curl -b cookies.txt -X POST 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device' \
  -H 'Content-Type: application/json' \
  -d '{"PageIndex": 0, "PageSize": 20}'
```

**Réponse `200 OK`:**

```json
{"Items": [], "TotalCount": 0, "PageIndex": 0, "PageSize": 20, "Code": "OK", "Succeeded": true}
```

***

### POST `/api/v1/device/sendCommand`

**Request:**

```bash
curl -b cookies.txt -X POST 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device/sendCommand' \
  -H 'Content-Type: application/json' \
  -d '{"PageIndex": 0, "PageSize": 20}'
```

**Réponse `200 OK`:**

```json
{"Items": [], "TotalCount": 0, "PageIndex": 0, "PageSize": 20, "Code": "OK", "Succeeded": true}
```

***

### DELETE `/api/v1/device/removeDevices`

**Request:**

```bash
curl -b cookies.txt -X DELETE 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device/removeDevices'
```

**Réponse `200 OK`:**

```json
{"Code": "OK", "Succeeded": true, "Message": null}
```

***

### POST `/api/v1/device/updateData`

**Request:**

```bash
curl -b cookies.txt -X POST 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device/updateData' \
  -H 'Content-Type: application/json' \
  -d '{"PageIndex": 0, "PageSize": 20}'
```

**Réponse `200 OK`:**

```json
{"Items": [], "TotalCount": 0, "PageIndex": 0, "PageSize": 20, "Code": "OK", "Succeeded": true}
```

***

### POST `/api/v1/device/update`

**Request:**

```bash
curl -b cookies.txt -X POST 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device/update' \
  -H 'Content-Type: application/json' \
  -d '{"PageIndex": 0, "PageSize": 20}'
```

**Réponse `200 OK`:**

```json
{"Items": [], "TotalCount": 0, "PageIndex": 0, "PageSize": 20, "Code": "OK", "Succeeded": true}
```

***

### POST `/api/v1/device/search`

**Request:**

```bash
curl -b cookies.txt -X POST 'https://<your-server>/rdx/NDS.Services.Device/api/v1/device/search' \
  -H 'Content-Type: application/json' \
  -d '{"PageIndex": 0, "PageSize": 20}'
```

**Réponse `200 OK`:**

```json
{"Items": [], "TotalCount": 0, "PageIndex": 0, "PageSize": 20, "Code": "OK", "Succeeded": true}
```

***

> Les points de terminaison supplémentaires suivent les mêmes modèles que ceux présentés ci-dessus. Consultez l'interface Swagger UI pour les schémas de paramètres complets.
